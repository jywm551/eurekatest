server:
  port: 8000
spring:
  application:
    name: provider
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    platform: h2
    schema: classpath:schema.sql
    data: classpath:data.sql
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
logging:
  level:
    root: INFO
    org.hibernate: INFO
    org.hibernate.type.secriptor.sql.BasicBinder: TRACE
    org.hibernate.type.secriptor.sql.BasicExtractor: TRACE
